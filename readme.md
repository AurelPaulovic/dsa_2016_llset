This is a simple and not yet finalized implementation of an ordered integer set.

The data structure is implemented as a singly-linked list of integers.

Compile & run
==============
    > gcc -o app *.c
    > ./app 
